@model ProductPriceModel

@using Nop.Core
@using Nop.Core.Domain.Tax

@inject IWorkContext WorkContext

@if (!Model.CustomerEntersPrice)
{
    <div class="prices d-flex align-items-center">
        @if (Model.CallForPrice)
        {
            @*call for price*@
            <div class="product-price call-for-price">
                <span>@T("Products.CallForPrice")</span>
            </div>
        }
        else
        {
            if (Model.IsRental)
            {
                <div class="rental-price">
                    <span>@T("Products.Price.RentalPrice"):</span>
                    <span>@Model.RentalPrice</span>
                </div>
            }
            if (!string.IsNullOrWhiteSpace(Model.PriceWithDiscount))
            {
                <text></text>
            }
            else if (!string.IsNullOrWhiteSpace(Model.OldPrice))
            {
                @*old price*@
                <div class="old-product-price">
                    @* <span>@T("Products.Price.OldPrice"):</span> *@
                    <s>@Model.OldPrice</s>
                </div>
            }
            <div class="@if (!string.IsNullOrWhiteSpace(Model.PriceWithDiscount))
                        {
                            <text>product-price</text>
                        }
                        else
                        {
                            <text>non-discounted-price</text>
                        }">
                @if (!string.IsNullOrWhiteSpace(Model.PriceWithDiscount))
                {
                    <s class="price-in-cart h4 fw-bold">@Html.Raw(Model.OldPrice)</s>
                }
                else {
                    @*render price*@
                    <span  
                            @if (!string.IsNullOrWhiteSpace(Model.OldPrice) || !string.IsNullOrWhiteSpace(Model.PriceWithDiscount)) 
                            { 
                                <text> id="price-value-@(Model.ProductId)" class="actual-price price-value-@(Model.ProductId) margin-inline-start-2 h4 fw-bold" </text>  
                            }
                            else
                            {
                                <text> id="price-value-@(Model.ProductId)" class="price-value-@(Model.ProductId) h4 fw-bold" </text>
                            }
                    >
                        @Html.Raw(Model.Price)
                    </span>
                }
            </div>
            if (!string.IsNullOrWhiteSpace(Model.PriceWithDiscount))
            {
                <div class="margin-inline-start-4">
                    <span class="lower-price-in-cart fw-bold">See Lower Price in Cart</span>
                </div>
            }
            if (!string.IsNullOrEmpty(Model.BasePricePAngV))
            {
                <div class="base-price-pangv" id="base-price-pangv-@Model.ProductId">
                    @Model.BasePricePAngV
                </div>
            }
            if (Model.DisplayTaxShippingInfo)
            {
                var inclTax = await WorkContext.GetTaxDisplayTypeAsync() == TaxDisplayType.IncludingTax;
                //tax info is already included in the price (incl/excl tax). that's why we display only shipping info here
                //of course, you can modify appropriate locales to include VAT info there
                <div class="tax-shipping-info">
                    @T(inclTax ? "Products.Price.TaxShipping.InclTax" : "Products.Price.TaxShipping.ExclTax", Url.RouteTopicUrl("shippinginfo"))
                </div>
            }
        }
    </div>
}
